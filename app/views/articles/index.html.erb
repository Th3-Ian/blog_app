<title>Rails Blog Application</title>

<h1>Hello, Rails!</h1>
<h2>Great Reads Below(not really)</h2>

<ul>
  <% @articles.each do |article| %>
  <li>
    <%= link_to article.title, article %>
  </li>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
